import { Header } from '../../components';
import { Main } from '../../components/AddedPolicy';

const HomePage: React.FC = () => {
  return (
    <div>
      <Header /> 
      <Main />
    </div>
  )
};

export default HomePage;