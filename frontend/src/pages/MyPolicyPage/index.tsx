import { Header } from '../../components';
import { Main } from '../../components/MyPolicyCards';

const HomePage: React.FC = () => {
  return (
    <div>
      <Header /> 
      <Main />
    </div>
  )
};

export default HomePage;